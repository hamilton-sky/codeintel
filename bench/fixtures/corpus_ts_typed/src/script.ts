// UNDECIDABLE — no import and no export, so this file is a SCRIPT, not a module. Its top-level
// names share the global scope, and bare names in it are not governed by module reachability.
function runAll() {
  return forwardReleasedItem("s");
}
